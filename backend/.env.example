PORT=5000
MONGODB_URI=mongodb+srv://waseemrana8819_db_user:cdkLjeH26FDfRAr8@cluster0.cndgc0n.mongodb.net/?appName=Cluster0

# Cloudinary (product image uploads)
# Cloud name and API key are already set as defaults in backend/config/cloudinary.js
# (rxtpqlsx / 265676185824175) since they are not secret. The API secret below is
# REQUIRED and must never be committed — get it from https://console.cloudinary.com
# (Dashboard -> Product Environment Credentials -> API Secret), then set it here
# for local dev AND in your Vercel Project Settings -> Environment Variables for
# production. Uploads will fail with a clear error until this is set.
CLOUDINARY_API_SECRET=